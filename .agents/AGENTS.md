# Workspace Rules

## Git & Deployment Workflow
- Sau khi hoàn thành xong bất kỳ yêu cầu, tính năng hoặc sửa lỗi nào, tự động thực hiện commit và push toàn bộ thay đổi lên GitHub (`git add .`, `git commit -m "..."`, `git push origin main`).
